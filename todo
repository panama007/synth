change osc to take in phase instead of freq
create entity for all variables (or put this in the top block)
add amplitudes, multiplying osc outputs (coeff < 1)
diff FM block architectures

amplitude envelope, as output of voice
change display to allow upside-down
    add entity outside of display that smartly chooses what to display
make base frequency NOT a mult of 2
filters?
finish up karplus

